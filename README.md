# Ahmed E. Mansour personal website

This repository now follows a simple Jekyll/GitHub Pages structure with a shared default layout and consistent navigation.

## Local preview

Use Ruby and Bundler:

```bash
gem install bundler
bundle install
bundle exec jekyll serve
```

Then open http://localhost:4000.
